{% extends "layout.twig" %}

{% block headLinks %}
    <link href="{{ alias('@web/css/daisyui-full.min.css') }}" rel="stylesheet" type="text/css"/>
    <script src="{{ alias('@web/js/tailwindcss.min.js') }}"></script>
{% endblock headLinks %}

{# -- Content that appears before the primary content block -- #}
{% block preContent %}
    <section>
        <div class="hero bg-base-200">
            <div class="hero-content text-center">
                <div class="max-w-md">
                    <h1 class="text-5xl font-bold">Simple reactive demo</h1>
                </div>
            </div>
        </div>
    </section>
{% endblock %}

{# -- Primary content block -- #}
{% block content %}
    <section data-store="{ input: '', characterCount: 0}">
        <div class="text-center">
            {#
            <button
                class="btn btn-primary m-3"
                data-on-click="$input='woof'; alert('Set it to woof!');">Set it to 'woof'!
            </button>
            #}
            <h3 class="text-3xl p-3">Type something:</h3>
            <input
                class="input input-bordered w-full max-w-xs"
                data-model="input"
                data-on-keydown="$characterCount = $characterCount + 1;"
                placeholder=""
                type="text"
            />
            <p class="text-3xl p-3">
                <span data-text="$input"></span>
                <span data-show="$input == 'Toronto'">🇨🇦</span>
            </p>
        </div>
    </section>
{% endblock %}

{# -- Content that appears after the primary content block -- #}
{% block postContent %}
    <section class="text-center">
        <p>Characters types: <span data-text="$characterCount">0</span></p>
    </section>
{% endblock %}
