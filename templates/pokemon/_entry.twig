{% extends "layout.twig" %}

{# -- Content that appears before the primary content block -- #}
{% block preContent %}
    {{ include("_includes/site-header.twig", { pageTitle: entry.pokemonName}, with_context = false) }}
{% endblock %}

{# -- Primary content block -- #}
{% block content %}
    {{ include("_includes/pokemon-detail.twig", { pokemon: entry }, with_context = false) }}
{% endblock %}
